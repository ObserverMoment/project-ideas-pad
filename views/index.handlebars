<div class="jumbotron jumbotron-fluid">
  <div class="container text-center">
    <h1 class="display-3">New Project Ideas Notepad</h1>
    <p class="lead">Notebook and sketchpad for money making schemes and planning heists.</p>
    {{#if user}}
      <h4>Hi, {{user.firstName}}!</h4>
      <a href="/ideas/add" class="btn btn-dark btn-lg">Add New Idea</a>
    {{else}}
      <a href="/users/login" class="btn btn-info btn-lg">Login</a>
      <a href="/users/register" class="btn btn-dark btn-lg">Register</a>
    {{/if}}
  </div>
</div>
